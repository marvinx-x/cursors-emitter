block vars
  - var nbrCursors = 4;

doctype html
html(lang="en")
  include head

  body(class="preload")
    include header

    main(role="main" id="swup")
      block main

    include footer
    #cannot-use
      p You can use this only on no touch devices

    script(type="module" src="index.js")
